export enum DeveloperStatusLowerCase {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
}

export enum VerificationStatus {
  ONBOARDING = 'ONBOARDING',
  SYSTEM_WIP = 'SYSTEM_WIP',
  DATA_READY = 'DATA_READY',
  DATA_RETURNED = 'DATA_RETURNED',
  VERIFIED = 'VERIFIED',
}

export enum SearchKeyType {
  FULL_NAME = 'FULL_NAME',
  NIK = 'NIK',
  PHONE = 'PHONE',
  EMAIL = 'EMAIL',
}
